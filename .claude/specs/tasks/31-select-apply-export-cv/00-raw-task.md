Task 31: Select and apply optimizations, export optimized cv to pdf and docx

Description:

The optimization process produces AI results across 6 panels. Currently they are display-only. The goal is to let users select specific suggested changes (summary variant, bullet rewrites, missing keywords), allow user to modify the suggestions and apply them to a modified copy of their CV, and export that CV as PDF or DOCX. 

1. Summary rewriten - allow selecting summary and editing

- allow user to select one of presented summary versions,
- display it in textarea so that user can edit it
- add button 'Apply selected version" to save it in db optimization_results for given prompt type in column userEditedOutput.

2. Keyword gap - allow selecting, modifying and applying it to cv.

3. Bullet upgrades - allow selecting, modifying and applying suggestion to cv.

4. Export optimized cv to pdf and docx