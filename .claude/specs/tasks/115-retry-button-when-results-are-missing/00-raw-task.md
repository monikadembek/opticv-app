Task 115. Implement Retry functionality if some optimalization section has not been processed or has no results

Description:

If some optimalization sections have no results, because there has been some error, or user might have close the app by mistake and not all jobs run, we should display Retry button and allow running this single job within the quota limits.