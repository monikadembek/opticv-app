Task 39: Cv Optimizations - show list, delete optimization, open stored cv optimization

Description:
Curently user can only run the process of cv optimization. User doesn't have the functionality to check previous optimizations. We need to add the functionality to check previous optimizations.

To do:
- In Dashboard show list of previous optimizations for given user.
- Add functionality to delete previous optimizations
- add functionality to open and display previous optimization together with provided job application informations and selected cv.

We will need backend endpoints for that and frontend implementation as well.