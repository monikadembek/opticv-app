Task 33. Rate limiting: Per-user and per-IP, on both API and AI calls.

Description:
Implement Rate limiting: Per-user and per-IP, on both API and AI calls. Critical from day one.

1. Implement per-user rate limiting
2. Implement per-IP rate limiting