Task 57: LinkedIn profile update - generate new version of prompt for LINKEDIN_REWRITE (BE)

Description:

Genarete new version of prompt for the LINKEDIN_REWRITE.

We already have LINKEDIN_REWRITE prompt in table prompt_versions, but it requires user to provide information from their current linkedin profile and rewrite against it. 
For now we want to simplify things regarding the linkedin optimization and not require user to provide data from their current profile page but instead the linkedin profile information should be prepared based on the selected cv and job position / job description. 

Generate new version of LINKEDIN_REWRITE prompt that will generate linkedin prifile informations without requiring user to provide previous profile data but only data from selected cv and eventually the job description/job position data will be used. Set the prompt version to 2.0.0.

Don't try to make migration alone. Let me do it manually. Because previously there have been issues, and there was a lock put.
