Task 50: Job posting visible after upload

Description:
Currently we can see job posting form only in the inital state in the cv-optimization page. When user runs the optimization process it is no longer displayed, but user must be able to see the job posting data.
Implement the functionality to display job posting data after the optimization process finishes, display either the job-upload form filled with the data that user just pasted or display the job-info-banner.
At the top on the right side put a button with label 'New Optimization', when user clicks that button it should open the cv-optimization page with initial state.